package upd;

/* Just a placeholder for testing purposes,
 * 
 * Make the typedef in UniformPoissonDisk.hx point to the real class (e.g. flash.geom.Point)!
 */

class SimplePoint {
  public var x:Float;
  public var y:Float;
  
  public inline function new(x:Float, y:Float):Void {
    this.x = x;
    this.y = y;
  }
}